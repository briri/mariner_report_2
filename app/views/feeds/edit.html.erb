<div class="admin-form">
  <h2 class="article-title">
    Editing Feed For - 
		<a href="<%= edit_publisher_path(@feed.publisher.slug)%>" class="admin-links"><%= @feed.publisher.name %></a>
  </h2>
  
  <%= render partial: 'form' %>

  <div class="tabular-association">
    <%= render partial: 'shared/articles', 
               locals: {articles: @articles} %>
  </div>
</div>