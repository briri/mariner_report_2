<div>
  <h2 class="article-title">The Week in Sailing</h2>
	<h4>Mon. <%= "#{@l.month}/#{@l.day}" %> - Mon. <%= "#{@n.month}/#{@n.day}" %></h4>
	<hr />
  <%= render partial: 'shared/articles', 
             locals: {articles: @articles.order(publication_date: :desc)} %>
</div>